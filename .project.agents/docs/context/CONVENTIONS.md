# AIMS — 开发规范（CONVENTIONS）

> 命名、目录、提交、风格、测试的权威。新约定先写本文，再落地代码。
> 与命名/目录相关的架构调整，`ARCHITECTURE.md` 与本文同步更新。

## 0. 阅读指引
<!-- ADS:FILL 本文适用范围；与 ARCHITECTURE 的分工（ARCH 管"拆成什么模块"，本文管"怎么命名/写"）。 -->

## 1. 命名

### 1.1 标识符
<!-- ADS:FILL 语言惯例：类型/函数/变量/常量的大小写风格。 -->

### 1.2 模块命名（语义禁区）
- 禁止 `Manager` / `Helper` / `Util` / `Common` / `Misc` / `Tools` 这类语义为空的名字。
- 用"动词+名词"描述真实职责（如 `StreakCalculator`、`CheckInService`）。

### 1.3 文件 / 1.4 目录 / 1.5 资产命名
<!-- ADS:FILL 文件按主类型命名；目录约定；资产命名规则（如有）。 -->

### 1.6 字符串与本地化
<!-- ADS:FILL 用户可见字符串归属哪个文件；是否禁止硬编码。 -->

## 2. 代码风格

### 2.1 排版
<!-- ADS:FILL 缩进宽度、行宽、括号风格。 -->

### 2.2 分层纪律
<!-- ADS:FILL UI 只消费状态/发意图；业务逻辑放 Service/UseCase；模型保持瘦。 -->

### 2.3 并发 / 2.4 错误处理
<!-- ADS:FILL 并发模型约定；错误只在系统边界防御，内部模块互信。 -->

### 2.5 注释
- 默认不写注释；只在 WHY 不明显时写一行。
- 不写解释 WHAT 的注释（命名应自解释），不写会随时间失效的注释。

## 3. 提交规范

### 3.1 Commit message 风格
<!-- ADS:FILL 推荐 Conventional Commits：type(scope): subject。列允许的 type。 -->
推荐 Conventional Commits：`feat` / `fix` / `docs` / `refactor` / `test` / `chore` …

### 3.2 提交粒度 / 3.3 工作区纪律 / 3.4 分支模型
- 一次提交一个目的；不在脏工作区叠加不相关改动。
- 分支模型：{{BRANCH_MODEL}}（最简：主干 + 短命功能分支）。

### 3.5 实施日志
- 规则见 `VIBECODING_GUIDE §5`。每个可独立验收的部分写一份日志。

## 4. 测试策略

### 4.1 框架 / 4.2 必须有测试的模块
<!-- ADS:FILL 测试框架；哪些模块必须有单测（纯计算、关键服务）。 -->

### 4.3 必须真机/真环境验证
<!-- ADS:FILL 哪些场景 mock/模拟器不算数，必须真实环境验证。 -->

## 5. 与上游文档的同步矩阵
<!-- ADS:FILL 哪类改动要同步更新哪个文档（精简版 VIBECODING_GUIDE §4）。 -->

## 6. 不在本文范围（明确划清）
<!-- ADS:FILL 本文不管什么（如：模块拆分归 ARCHITECTURE，行为归 PRD）。 -->
