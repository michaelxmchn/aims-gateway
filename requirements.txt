# Form / multipart upload support (required by /api/skills/upload)
python-multipart>=0.0.19,<1.0

# Core web framework
fastapi==0.134.0
starlette==0.52.1
uvicorn[standard]==0.41.0

# Schema / validation
pydantic==2.12.5
pydantic_core==2.41.5

# Redis persistence (Fly.io)
redis>=5.0,<6.0
hiredis>=2.0,<4.0

# Worker HTTP client
requests>=2.31,<3.0

# Worker browser automation (Playwright for scraping tasks)
playwright>=1.40,<2.0

# OpenClaw agent orchestration
openclaw>=2026.3,<2027.0
