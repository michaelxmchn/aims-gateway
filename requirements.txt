# Form / multipart upload support (required by /api/skills/upload)
python-multipart>=0.0.19,<1.0

# Core web framework
fastapi==0.134.0
starlette==0.52.1
uvicorn[standard]==0.41.0

# Schema / validation
pydantic==2.12.5
pydantic_core==2.41.5

# Redis persistence (Fly.io)
redis>=5.0,<6.0
hiredis>=2.0,<4.0

# Worker HTTP client
requests>=2.31,<3.0

# Worker browser automation (Playwright for scraping tasks)
playwright>=1.40,<2.0

# OpenClaw agent orchestration
openclaw>=2026.3,<2027.0

# Multimodal inputs (image processing, OCR, base64)
opencv-python-headless>=4.9,<5.0
pillow>=10.0,<12.0
pytesseract>=0.3,<1.0

# Web3 / EVM dependencies (EIP-712 signing, on-chain settlement)
web3>=7.0,<8.0
eth-account>=0.12,<1.0
eth-hash[pycryptodome]>=0.7,<1.0
